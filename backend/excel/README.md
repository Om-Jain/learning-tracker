Excel file access is handled through `backend/utils/bootstrapExcel.js` and `backend/utils/excelStore.js`.

The actual `.xlsx` workbooks are created in the project-level `excel-storage/` directory when the backend starts.
